import type { AnimationDescriptor } from "../types/animation";

const hoverFloat: AnimationDescriptor[] = [

    {
        type: "float",
        speed: 2,
        intensity: 0.35,
    },

    {
        type: "pulse",
        speed: 2.5,
    }

];

export default hoverFloat;